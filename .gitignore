# Python
.venv/
.venv-cv/
.venv-fish/
__pycache__/
*.pyc

# Secrets
.elevenlabs_key
.gemini_key

# Local-only assets / experiment artifacts
voices/
checkpoints/

# Local working data (not published)
data/inbox/
data/articles/
data/pending.json
data/_wav/
data/tts_smoketest.wav

# Keep tracked (the record + what gets served):
#   data/state.json
#   data/summaries/
#   docs/  (feed.xml, cover.png, audio/*.mp3)
